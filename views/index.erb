<!DOCTYPE html>
<html lang="ja">
    <head>
        <title>おみくじ</title>
        <meta charset="UTF-8">
    </head>
    <body>
        <h1>おみくじ</h1>
        <% if params['act'] != nil %>
            <% fortune = ['大吉', '中吉', '小吉', '末吉', '吉', '凶'] %>
            <% result = fortune[rand(0..5)] %>
        <% else %>
            <% result = 'ありません' %>
        <% end %>
        <p>おみくじの結果：<%= result %></p>
        <form action="/" method="POST">
            <button type="submit" name="act" value="draw">おみくじをひく！</button>
        </form>
    </body>
</html>